---
name: gen-gitlab-ci
description: Generate GitLab CI/CD pipeline configuration for Go projects with testing, coverage, and automated releases
argument-hint: "[--force] [--dry-run]"
allowed-tools: Read, Write, Bash(git:*), Bash(test:*), AskUserQuestion
---

# Generate GitLab CI Configuration Command

Instantly create a `.gitlab-ci.yml` file for Go projects with production-ready GitLab CI/CD pipeline including unit tests, optional coverage reporting, snapshot builds, and automated releases.

## Why This Command Exists

**Problem**: Setting up GitLab CI/CD for Go projects requires manually creating pipeline configuration, understanding GitLab CI syntax, and configuring Docker-in-Docker - a 30-45 minute process.

**Solution**: One-command generation that:
- Creates complete `.gitlab-ci.yml` configuration (139 lines)
- Includes comprehensive pipeline: unit tests, coverage, snapshot builds, releases
- Configures Docker-in-Docker for container builds
- Supports multi-registry publishing (GitLab + external)
- Preserves all customization comments for user control

## Content Generation Rules

**CRITICAL**: When generating any content (pipeline files, commit messages, or any other output):
- **NEVER** add Claude Code attribution or co-authorship
- **NEVER** include phrases like "Generated by Claude", "Created with Claude Code", or similar
- **NEVER** add "Co-Authored-By: Claude" in commit messages or documentation
- All generated content should appear as if written by a human developer
- Focus on technical accuracy and providing working templates

## Process (6 Phases)

### Phase 1: Prerequisites Validation

**Critical checks before proceeding:**

1. **Validate Git repository:**
   ```bash
   git rev-parse --git-dir
   ```
   - If fails: Exit with error "Not a git repository. Run 'git init' first."

2. **Validate Go project:**
   ```bash
   test -f go.mod
   ```
   - If fails: Exit with error "go.mod not found. This command is for Go projects only."

3. **Check existing .gitlab-ci.yml:**
   ```bash
   test -f .gitlab-ci.yml
   ```
   - If exists: Store `existingGitlabCi = true`
   - If not exists: Store `existingGitlabCi = false`

4. **Verify command assets directory:**
   - Use Read tool to verify `${CLAUDE_PLUGIN_ROOT}/commands/assets/gitlab-ci/.gitlab-ci.yml` exists
   - If fails: Exit with error "Command assets directory not found at ${CLAUDE_PLUGIN_ROOT}/commands/assets/gitlab-ci/. Plugin may be corrupted. Reinstall the go-specialist plugin."

5. **Check for .goreleaser.yml (non-blocking):**
   ```bash
   test -f .goreleaser.yml
   ```
   - If missing: Store `goreleaserMissing = true` (warn in Phase 6, don't block creation)
   - If exists: Store `goreleaserMissing = false`

### Phase 2: Argument Parsing

Parse command arguments from `$argument` string:

**Supported flags:**
- `--force`: Skip confirmation for existing .gitlab-ci.yml
- `--dry-run`: Show preview without writing file

**Parsing logic:**
```
arguments = split($argument by space)
for each arg in arguments:
  if arg == "--force": set forceMode = true
  if arg == "--dry-run": set dryRunMode = true
```

**No minimal mode:** Template is already compact at 139 lines with comprehensive coverage.

### Phase 3: Template Reading

**Read template file** from command assets directory:

**File to read:**

1. **.gitlab-ci.yml** (139 lines)
   - Path: `${CLAUDE_PLUGIN_ROOT}/commands/assets/gitlab-ci/.gitlab-ci.yml`
   - Target: `.gitlab-ci.yml` (project root)

**Template handling:**
- Copy file as-is (no variable substitution)
- Preserve all `# CUSTOMIZE:` comments
- Store content in memory for Phase 5 writing

**Error handling:**
- If Read fails: Exit with error "Failed to read template file. Plugin may be corrupted."

### Phase 4: User Confirmation

**Build preview summary:**

```
File to be created:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ .gitlab-ci.yml (139 lines)

Pipeline Structure:
- 4 Stages: build, test, release, deploy
- 4 Jobs:
  • unit-tests: Run on every push
  • coverage: Optional (currently commented out)
  • build-snapshot: Test releases without publishing
  • build-release: Production releases on Git tags

[If existingGitlabCi == true:]
⚠️  WARNING: .gitlab-ci.yml already exists and will be overwritten

[If goreleaserMissing == true:]
⚠️  NOTE: .goreleaser.yml not found
    Pipeline jobs will require this file to function.
    Generate with: /goreleaser
```

**Dry run mode:**
If `dryRunMode == true`:
- Show preview summary above
- Display first 30 lines of .gitlab-ci.yml
- Show customization requirements
- Display next steps guide
- Exit without creating file

**Confirmation logic:**

1. **If `--force` flag provided:** Skip confirmation, proceed to Phase 5

2. **If existing .gitlab-ci.yml without `--force`:**
   ```
   Use AskUserQuestion:
   Question: ".gitlab-ci.yml already exists. Overwrite existing pipeline configuration?"
   Options:
   1. "Yes, overwrite file" [Recommended if upgrading pipeline]
   2. "Show full preview first"
   3. "Cancel"

   If option 2: Display full content preview, then re-ask confirmation
   If option 3: Exit gracefully
   ```

3. **If no .gitlab-ci.yml exists:**
   ```
   Use AskUserQuestion:
   Question: "Create .gitlab-ci.yml at project root?"
   Options:
   1. "Yes, create file"
   2. "Show preview first"
   3. "Cancel"
   ```

### Phase 5: File Creation

**File writing:**

Write `.gitlab-ci.yml` to current working directory (project root):
- Use Write tool with file path: `.gitlab-ci.yml`
- Write content from Phase 3 template (no modifications)
- Track success/failure status

**Error handling:**
- If write fails: Exit with error message detailing failure
- If successful: Proceed to Phase 6 validation

### Phase 6: Validation & Success Report

**Post-creation validation:**

1. Verify file exists: `test -f .gitlab-ci.yml`
2. Verify non-zero size: File should be > 0 bytes
3. If validation fails: Report error and exit

**Generate success report:**

```
[If file created successfully:]
✅ GitLab CI Configuration Created Successfully
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Created File:
✅ .gitlab-ci.yml (139 lines)

Pipeline Structure:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 4 Stages: build, test, release, deploy
🔧 4 Jobs:
   - unit-tests: Run on every push
   - coverage: Optional (currently commented out)
   - build-snapshot: Test releases without publishing
   - build-release: Production releases on Git tags

Required Customizations:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  PRIORITY 1 - Must configure for pipeline to work:

1. Configure GitLab Runner tags (4 locations) - MOST CRITICAL:
   - Find: tags: [gitlab-org-docker]
   - Replace: With your runner tags
   - How to find tags: GitLab Settings → CI/CD → Runners
   - Common tags: docker, linux, kubernetes

2. Update Go version (4 locations):
   - Find: image: golang:1.25.1
   - Replace: With your Go version (e.g., golang:1.24.2)

3. Update GoReleaser version (2 locations):
   - Find: goreleaser/goreleaser:v2.12.0
   - Replace: With desired version or 'latest'

4. Enable coverage job (optional):
   - Uncomment lines 74-92 in .gitlab-ci.yml
   - Configure coverage exclusions if needed

5. Configure external Docker registry (optional):
   - GitLab Settings → CI/CD → Variables
   - Add: EXTERNAL_CI_REGISTRY, EXTERNAL_CI_REGISTRY_USER, EXTERNAL_CI_REGISTRY_PASSWORD

GitLab CI/CD Variables (Auto-configured):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ CI_REGISTRY: GitLab Container Registry URL
✅ CI_REGISTRY_IMAGE: Your project's registry path
✅ CI_REGISTRY_USER: Auto-authenticated as gitlab-ci-token
✅ CI_JOB_TOKEN: Auto-generated token for registry push

GoReleaser Configuration Status:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[If goreleaserMissing == false:]
✅ .goreleaser.yml found - Release jobs ready to use

[If goreleaserMissing == true:]
⚠️  .goreleaser.yml not found
    - build-snapshot job will fail without this file
    - build-release job will fail without this file

    Generate now: /goreleaser
    Or create manually: https://goreleaser.com/quick-start/

Common Issues & Troubleshooting:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ "No such runner tags: gitlab-org-docker"
   → Update runner tags in .gitlab-ci.yml (4 locations)
   → Check available tags: Settings → CI/CD → Runners

❌ "goreleaser: config file not found"
   → Generate .goreleaser.yml with /goreleaser skill
   → Or create manually at project root

❌ "Docker login failed" in pipeline jobs
   → Enable Container Registry: Settings → General → Visibility
   → Verify Docker-in-Docker service configured in runner

Next Steps:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Review and customize .gitlab-ci.yml
2. Ensure .goreleaser.yml exists (use /goreleaser skill if needed)
3. Commit changes:
   git add .gitlab-ci.yml
   git commit -m "ci: add GitLab CI/CD pipeline"

4. Push to GitLab:
   git push origin main

5. Verify pipeline:
   - GitLab → CI/CD → Pipelines
   - Pipeline should run automatically on push

Documentation:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Pipeline configuration reference: .gitlab-ci.yml inline comments
- GoReleaser setup: Use /gen-goreleaser command for .goreleaser.yml
- Runner configuration: GitLab Settings → CI/CD → Runners

[If file creation failed:]
❌ GitLab CI Configuration Creation Failed
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Error: [Error message details]

Possible causes:
- Insufficient permissions
- Disk space issues
- File system restrictions

Action: Check errors above and ensure write permissions in current directory
```

## Command Arguments

Arguments are parsed from the `argument` string parameter:

- **`--force`**: Skip confirmation prompt and overwrite existing file
  - Use when: Upgrading existing pipeline to latest template
  - Behavior: Overwrites .gitlab-ci.yml directly without confirmation

- **`--dry-run`**: Preview file without writing to disk
  - Shows: File preview, pipeline structure, customization requirements
  - Writes: Nothing
  - Use when: Reviewing what will be created before committing

**Parsing example:**
```
argument = "--force"
Split by space, check for each flag:
- forceMode = true
- dryRunMode = false
```

## Error Handling

### Critical Errors (Exit Immediately)

**Not a Git repository:**
```
❌ Error: Not a git repository

This command requires a Git repository. Initialize one:
  git init
  git add .
  git commit -m "Initial commit"
```

**Not a Go project:**
```
❌ Error: go.mod not found

This command is for Go projects only. Initialize a Go module:
  go mod init gitlab.com/username/project
```

**Skill assets missing:**
```
❌ Error: Skill assets directory not found

The go-specialist plugin may be corrupted. Try:
1. Reinstalling the plugin
2. Updating Claude Code to latest version
3. Reporting issue at: https://github.com/sgaunet/claude-plugins/issues
```

### Warning Errors (Continue with Warnings)

**Existing .gitlab-ci.yml:**
- Without `--force`: Request user confirmation
- With `--force`: Overwrite silently

**Missing .goreleaser.yml:**
- Don't block creation
- Warn in success report
- Suggest using /goreleaser skill

### Recovery Actions

**If file write fails:**
```
❌ Error: Failed to write .gitlab-ci.yml

Check:
1. Current directory is writable
2. Sufficient disk space
3. No file system restrictions

Current permissions: [show with ls -ld .]
```

## File Mapping

| Source Path | Target (Relative to CWD) | Lines |
|-------------|--------------------------|-------|
| `${CLAUDE_PLUGIN_ROOT}/commands/assets/gitlab-ci/.gitlab-ci.yml` | `.gitlab-ci.yml` | 139 |

## Integration with Other Commands

**Works with:**
- `/commit`: Commit generated .gitlab-ci.yml
- `/gen-goreleaser`: Generate .goreleaser.yml config (required for release/snapshot jobs)
- `/create-issue`: Create issue to track customization tasks

**Typical Workflow:**
```bash
# 1. Generate GitLab CI configuration
/gen-gitlab-ci

# 2. Generate GoReleaser config (required)
/goreleaser

# 3. Customize files
# Edit .gitlab-ci.yml: Update runner tags, Go version
# Edit .goreleaser.yml: Configure builds and Docker images

# 4. Commit everything
git add .gitlab-ci.yml .goreleaser.yml
git commit -m "ci: add GitLab CI/CD pipeline"

# 5. Push and verify
git push origin main
# Check: GitLab → CI/CD → Pipelines
```

## Prerequisites for Pipeline

**Required for pipeline to function:**

1. **.goreleaser.yml** configuration:
   - Generate with `/goreleaser` command
   - Required for build-snapshot and build-release jobs

2. **GitLab Runner** with Docker executor:
   - Runner must have Docker-in-Docker capability
   - Update runner tags in .gitlab-ci.yml (4 locations)
   - Check available runners: Settings → CI/CD → Runners

3. **GitLab Container Registry** enabled:
   - Settings → General → Visibility, project features, permissions
   - Enable "Container Registry"

**Optional enhancements:**

1. **Coverage reporting:**
   - Uncomment coverage job in .gitlab-ci.yml (lines 74-92)
   - Configure coverage badge in README.md

2. **External Docker registry:**
   - Add CI/CD variables: EXTERNAL_CI_REGISTRY, EXTERNAL_CI_REGISTRY_USER, EXTERNAL_CI_REGISTRY_PASSWORD
   - Settings → CI/CD → Variables

3. **Custom tag patterns:**
   - Edit build-release job in .gitlab-ci.yml
   - Adjust `only: tags` to filter specific patterns

## Template Customization Points

All template files include `# CUSTOMIZE:` comments marking required changes:

**.gitlab-ci.yml:**
- `tags: [gitlab-org-docker]` (4 locations) → Replace with your runner tags
- `image: golang:1.25.1` (4 locations) → Update to your Go version
- `goreleaser/goreleaser:v2.12.0` (2 locations) → Update GoReleaser version
- Coverage job (lines 74-92) → Uncomment to enable
- Coverage exclusions → Configure sed patterns if needed
- Tag filtering → Adjust in build-release job

## Best Practices

**Before running command:**
1. Ensure git repository initialized
2. Have go.mod configured
3. Review pipeline configuration inline comments in template
4. Check available GitLab Runners and their tags

**After generation:**
1. Review all `# CUSTOMIZE:` comments in .gitlab-ci.yml
2. Generate .goreleaser.yml with /goreleaser skill
3. Update runner tags (most critical customization)
4. Update Go version to match your project
5. Commit with meaningful message
6. Monitor first pipeline run for errors

**Ongoing maintenance:**
1. Keep GoReleaser version updated
2. Review pipeline efficiency and optimize as needed
3. Adjust coverage thresholds as codebase matures
4. Add custom pipeline jobs as project needs evolve

## Limitations

**This command does NOT:**
- Generate .goreleaser.yml (use `/goreleaser` command)
- Modify existing .gitlab-ci.yml intelligently (use `--force` to overwrite)
- Validate that pipeline will work (requires proper runner setup)
- Configure GitLab repository settings (must be done via UI)
- Create GitLab Runners (requires infrastructure setup)

**Scope boundaries:**
- Only handles `.gitlab-ci.yml` file
- Template is opinionated (aligned with Go best practices)
- Assumes standard Go project structure
- Requires manual customization post-generation
- Requires Docker-in-Docker capable runners

## Success Criteria

✅ Prerequisites validated (git repo, Go project)
✅ Template file read from skill assets
✅ User confirmation obtained (unless `--force`)
✅ .gitlab-ci.yml written successfully
✅ File validated (exists and non-zero size)
✅ Comprehensive success report displayed
✅ Customization instructions provided
✅ Next steps guide shown
✅ No Claude Code attribution in generated content
✅ Total runtime < 5 seconds

## Examples

```bash
# Standard generation
/gen-gitlab-ci

# Preview before creating
/gen-gitlab-ci --dry-run

# Overwrite existing pipeline
/gen-gitlab-ci --force

# Force with dry run (preview what would be overwritten)
/gen-gitlab-ci --force --dry-run
```

## Output Summary

**Standard mode (default):**
- 1 file created
- 139 total lines
- Comprehensive CI/CD pipeline with 4 stages and 4 jobs

**Dry run mode (`--dry-run`):**
- 0 files created
- Preview shown
- Safe exploration
